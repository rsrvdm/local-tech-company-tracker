import type {Metadata} from 'next';import './globals.css';export const metadata:Metadata={title:'MWC Operating System · MidCoast Web Co.',description:'Prospects, audits, demos, proposals, production, care plans and deployments for MidCoast Web Co.'};export default function Layout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
